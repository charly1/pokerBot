How to install:
- Install Anaconda
- Open the anaconda prompt
- Go to the repository
- type: conda env update -n root -f init/envoronment.yml   

How to build:
- type in the anaconda prompt: conda env export > environment.yml  
- copy the file in the repository